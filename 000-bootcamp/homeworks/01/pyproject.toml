[project]
name = "01"
version = "0.1.0"
description = "Module 1 Homework pipelines"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
  "pandas>=3.0.5",
  "psycopg[binary,pool]>=3.3.4",
  "pyarrow>=25.0.1",
  "python-dotenv>=1.2.3",
  "requests>=2.34.2",
  "sqlalchemy>=2.0.52",
  "tqdm>=4.70.0",
]

[dependency-groups]
dev = [
    "pgcli>=4.5.0",
]
